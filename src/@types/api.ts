export type InvestigationInspector = {
  nic: string;
  name: string;
  assignments: Array<any>; // Change any to relevant
};

export type Suspector = {
  id: number;
  nic: string;
  name: string;
  dob: string;
};
